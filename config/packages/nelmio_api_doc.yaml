nelmio_api_doc:
    documentation:
        info:
            title: Loyalty Points Collector
            description: Event-sourced CQRS demo — wallets, loyalty points and asynchronous transfers.
            version: 2.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas:
        path_patterns:
            - ^/api(?!/doc$)
