# Development-only overrides. Committed on purpose: no real secret belongs here,
# and a shared default keeps a fresh clone runnable without extra setup.
# Put anything genuinely secret in .env.local instead.

###> symfony/framework-bundle ###
APP_SECRET=7ed115e64ec0f4f05c7697e1a43a458b
###< symfony/framework-bundle ###
